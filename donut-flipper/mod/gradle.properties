org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Check https://fabricmc.net/develop for the current values before building.
# These are the pieces most likely to need bumping over time; nothing else in
# the project is tied to a Minecraft version.
minecraft_version=1.21.11
yarn_mappings=1.21.11+build.1
loader_version=0.17.2
fabric_version=0.135.0+1.21.11

mod_version=0.1.0
maven_group=dev.skullzz.donutflipper
archives_base_name=donutflipper
