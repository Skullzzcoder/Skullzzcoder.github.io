org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# ---------------------------------------------------------------------------
# IMPORTANT: verify these four values before your first build.
#
# Go to https://fabricmc.net/develop/ and pick 1.21.11. That page prints the
# exact yarn / loader / fabric-api versions for the release. Paste them here.
# The values below are placeholders in the right format -- if any of them is
# wrong, Gradle fails immediately with "Could not find <artifact>", which
# tells you precisely which line to correct.
# ---------------------------------------------------------------------------
minecraft_version=1.21.11
yarn_mappings=1.21.11+build.1
loader_version=0.17.2
fabric_version=0.135.0+1.21.11

mod_version=1.0.0
maven_group=dev.skullzz
archives_base_name=mirage
